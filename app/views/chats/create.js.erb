<!--/*.messageを部分更新。appendメソッドで要素を追加 */-->
$('.message').append("<th></th><th style='text-align: right;'><%= @chat.message %></th>");

<!--/*チャット投稿後、フォームの値に前回の投稿内容が残らないようにする */-->
$('input[type=text]').val("")