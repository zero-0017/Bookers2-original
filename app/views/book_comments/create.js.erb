$('.comment_area').html("<%= j(render 'book_comments/comment_list', book: @book) %>");
$('.comment_count').html("コメント数：<%= @book.book_comments.count %>");
<!--フォームを送信したあとにフォームの値を空にします-->
$('textarea').val('');

